# DEPRECATED — do not install this file into sites-enabled.
#
# Copying this (or any file with limit_req_zone / proxy_cache_path) into
# /etc/nginx/sites-enabled can break nginx -t for EVERY site, including safwap.
#
# Use the isolated config instead:
#   deploy/nginx/wap-manager.conf
#   → /etc/nginx/sites-available/wap-manager
#   → symlink sites-enabled/wap-manager
#
# Leave /etc/nginx/sites-enabled/safwap untouched.
